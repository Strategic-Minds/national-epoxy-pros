# NEP V2 Source Truth Map

1. Locked A-I visual references in `public/locked-references/`.
2. Locked workbook files in `docs/workbooks/`.
3. Route visual contract in `docs/locked-source/docs/V2_ROUTE_VISUAL_CONTRACT.md`.
4. Asset install map in `docs/locked-source/docs/V2_ASSET_INSTALL_MAP.md`.
5. Production bootstrap code only as a scaffold donor.

Do not use older wrong hero or layout pack unless it matches the locked A-I references.
