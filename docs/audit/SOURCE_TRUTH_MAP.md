# Source Truth Map

1. Locked visual references: `public/locked-references/A_homepage.png` through `I_contact.png`.
2. Route contract: `docs/route-map.md`.
3. Approved asset manifest: `asset-lock-manifest.*` and `nep_asset_lock_manifest.*`.
4. Validation posture: `FINAL_VALIDATION_REPORT.md` and `docs/XTREME_LOCAL_GREEN_AUDIT_REPORT.md`.
5. Vercel behavior: `vercel.json` uses `npm ci` and `npm run build`.
6. Digital bid endpoint is mock-safe: `app/api/digital-bid/route.ts`.

Do not replace locked references or approved brand/visual strategy without operator approval.
