# Vercel Preview Receipt

Status: NOT EXECUTED IN THIS SANDBOX

Required next step:

```bash
vercel
```

Then run:

```bash
BASE_URL="https://YOUR-VERCEL-PREVIEW.vercel.app" node scripts/capture-screenshots.mjs
```

Required proof:
- Preview URL
- Build log pass
- Route smoke against preview
- Screenshot receipt
- Form test receipt
- Final scorecard
# Vercel Preview Receipt

Status: pending live verification.

Required next proof:
- push the replacement commit to `main`
- confirm the existing Vercel project reaches `Ready`
- verify the deployment URL and `nationalepoxypros.com`
